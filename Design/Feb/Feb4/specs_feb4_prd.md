1 Chart Matrix 
1.1 Create a filter to easily distinquish different tasks per status. Under the title there should be three buttons
    - Done: Show only the tasks with status = Done 
    - To Do: Show only the tasks with status = To Do 
    - In Progress: Show only the tasks with status = In Progress

1.2 Tasks: When user clicks Close the system should monitor if there are any changes done and ask the user if they want to save their changes first.  
